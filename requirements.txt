# Inference + serving. Torch itself is installed from the CPU wheel index in
# the Dockerfile: the CPU wheels build a 2.29 GB image, where the default CUDA
# wheels would add several GB of nvidia libraries this project never uses.
ultralytics==8.4.120
fastapi==0.120.1
uvicorn[standard]==0.38.0
python-multipart==0.0.20
pillow==11.3.0
opencv-python-headless==4.12.0.88
numpy>=2.1,<3

# Tracking (ByteTrack) needs a linear-assignment solver; ultralytics will
# auto-install it at first use otherwise, which fails in a read-only container.
lap==0.5.13

# Ships a static ffmpeg with libx264 on every platform. OpenCV's own writer
# only reaches codecs that browsers refuse to play inline.
imageio-ffmpeg==0.6.0
